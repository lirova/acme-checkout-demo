{
  "name": "acme-checkout",
  "version": "1.0.0",
  "description": "Cart pricing utilities for the Acme storefront",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
